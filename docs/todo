1) confirm insert in server socket before broadcastMsg
2) bug: double msg on user join
3) bug: dobuble msg on first user msg
4) set key to room_id when rendering rooms
5) bug: code breaks if the user enters a room with no messages
6) bug: a new broadcast msg in a non-focused room is sometimes displayed only after a refresh
7) bug: after login need to refresh to see rooms 


































 
    

flex
padding margin
height width
positioning 
vh, vw, rem, em, px, %
colors, fonts